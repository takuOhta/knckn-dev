---
/**
 * astro
 */

/**
 * constants
 */

/**
 * layouts
 */
import Layout from '@layouts/Layout.astro'

/**
 * components
 */
/**
 * variables
 */
const PAGE_CLASSNAME = 'webgl'
---

<Layout title="webgl_school_004" description="" keywords="">
  <main class={`p-${PAGE_CLASSNAME}`}>
    <astro-canvas>
      <div class="canvas-wrapper"><canvas></canvas></div>
      <div class="image-wrap" data-giriddividedimage="container">
        <img src="/assets/images/webgl-school/004/Johannes_Vermeer_(1632-1675)_-_The_Girl_With_The_Pearl_Earring_(1665).jpg" alt="" class="bg" />
        <div class="images">
          {new Array(900).fill(0).map(() => <div class="image" data-giriddividedimage="image" />)}
        </div>
      </div>
    </astro-canvas>
  </main>
</Layout>

<style lang="scss" is:global>
  @import url('https://fonts.googleapis.com/css2?family=Jost:wght@600&display=swap');
  $PAGE_CLASSNAME: 'webgl';
  .p-#{$PAGE_CLASSNAME} {
    width: 100%;
  }

  .canvas-wrapper {
    position: fixed;
    top: 0;
    left: 0;
    display: block;
    width: 100vw;
    height: 100vh;
    z-index: 10;
    > canvas {
      width: 100%;
      height: 100%;
    }
  }
  .image-wrap {
    position: relative;
    margin: 10% 10%;

    .bg {
      display: block;
      width: 100%;
      height: auto;
      opacity: 0;
      visibility: hidden;
    }
    .images {
      position: absolute;
      inset: 0;
      display: grid;
      grid-template-rows: repeat(30, 1fr);
      grid-template-columns: repeat(30, 1fr);
      .image {
        grid-auto-flow: dense;
        width: 100%;
        height: 100%;
      }
    }
  }
</style>

<script>
  // /**
  // * @description クライアントサイドで実行されるスクリプトはここに記述します。
  // */
  import { GridDividedImage } from '@scripts/pages/webgl-school/004/GridDividedImage.ts'

  // canvasタグの設定
  class AstroCanvas extends HTMLElement {
    #rootElement: HTMLElement
    #canvas: GridDividedImage
    constructor() {
      super()
      this.#rootElement = this
      this.#canvas = new GridDividedImage({ element: this.#rootElement })
    }

    /**
     * init canvas
     */
    #initCanvas() {}
  }
  customElements.define('astro-canvas', AstroCanvas)
</script>
